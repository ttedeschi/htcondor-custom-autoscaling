rules:
  - seriesQuery: 'condor_slot_activity_idle'        
    resources:
      template: "<<.Resource>>"
    name:
      matches: "^(.*)"
      as: "${1}"
    metricsQuery: 'sum(<<.Series>>) by (job)' 
